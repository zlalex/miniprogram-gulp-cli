const request = {
  beforeHooks: [],
  afterHooks: [],
  async send(options) {
    let __options = options
  },
  execBeforeHook(options){

  },
  execAfterHook(options){

  },
  async registerHook(type){

  }
}

export default request
